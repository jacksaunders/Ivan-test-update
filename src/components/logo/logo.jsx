import React from 'react';

import IconLogo from '../../icons/icon-logo.svg';

const styles = {
    width: 130,
    fill: '#fb534a',
};

const Logo = () => <IconLogo style={styles} fill={'#fb534a'} />;

export default Logo;
